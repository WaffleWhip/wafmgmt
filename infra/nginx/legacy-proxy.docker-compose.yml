services:
  proxy:
    image: nginx:alpine
    container_name: proxy
    ports:
      - "80:80"
      - "1000:1000"
      - "1001:1001"
      - "1002:1002"
    volumes:
      - ../../data/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
